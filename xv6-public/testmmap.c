#include "types.h"
#include "stat.h"
#include "user.h"

int main(void) 
{
    // void * ret= mmap(); 
    // int * (ret)
    
    // printf(1, "The address pointed to by mmap was %d\n", (int *) ret);
    exit();
} 